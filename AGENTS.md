# Sanlyn API — Codex/Forge 常驻作业铁律

> 本文件是 codex（forge）在本仓库作业的**强制规则**，每次动手前必读必守。违反等于交付失败。

## 1. 500 行铁律（最高优先）
- **写/改任何文件前，先 `wc -l` 查该文件现有行数**，在回复里报出来。
- 改完后**任何单文件不得超过 500 行**。
- 若预判改动会让某文件超过 500 行：**先停下，在回复里明确说"XX 文件将超 500 行"，并给出拆模块方案**，拆完再写。**严禁硬塞进一个大文件。**
- 交付时**逐一列出每个改动/新增文件的最终行数**，自证都 ≤500。

## 2. 不可自审
- 你产出的代码一律由人复审，**不要假设已正确**。把不确定点、假设、未覆盖的边界在交付说明里列清楚。

## 3. 未经明确指示，不得副作用
- **不执行 SQL、不连生产库改数据、不部署、不 `git push`、不重启服务**，除非 brief 明确要你做。
- 迁移/脚本只**产出文件**交人工跑；迁移必须幂等可逆（`ADD COLUMN IF NOT EXISTS`、视图末尾追加列等）。

## 4. 单一数据源
- 不新建与现有真源重复的表/字段/接口；**复用现有真源**。改单据/账单先找现有 handler，别另起炉灶。

## 5. 财务安全轨
- 对外确认类接口**只写草稿/override 表，绝不写任何财务事实表**（金额、已付、已开票等）。
- token 保持 **fail-closed**（校验失败即拒，不放行）。
- 金额只认财务白名单口径；**报关金额不得低于商检金额**（业务铁律）。

## 6. 只动 brief 指定范围
- 本仓库有大量未提交改动，**只改 brief 点名的文件**，不顺手动别的、不格式化无关文件。

## 7. 交付格式
结尾固定给出：①改动文件清单+各自最终行数 ②做过的静态检查（`node --check` 等）③假设与未覆盖点 ④人工需执行的后续步骤（跑哪个迁移/脚本、怎么验证）。
